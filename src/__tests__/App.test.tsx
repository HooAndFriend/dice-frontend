// Imports
import { render, screen, fireEvent } from '@testing-library/react'

// To Test
import App from '../App'

// Tests
test('Renders main page correctly', async () => {
  // Setup
  render(<App />)
})
