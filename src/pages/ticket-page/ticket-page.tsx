// ** MUI Imports
import { Box, Typography } from '@mui/material'

interface PropsType {}

const TicketPageView = ({}: PropsType) => {
  return (
    <Box>
      <Typography>Ticket Page</Typography>
    </Box>
  )
}

export default TicketPageView
